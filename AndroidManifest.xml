<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.appoke.hello.world"
    android:versionCode="1"
    android:versionName="1.0" >
 	<uses-permission android:name="android.permission.INTERNET"/>
    <uses-sdk android:targetSdkVersion="14" android:minSdkVersion="3" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" 
        android:debuggable="false">
        <activity
            android:label="@string/app_name"
            android:name=".HelloWorldActivity" >
            <intent-filter >
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity
            
            android:launchMode="singleTask"
            android:name="com.appoke.sdk.ShareActivity"
            android:theme="@android:style/Theme.Dialog" >
            <intent-filter >
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
				<category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:host="@string/package_name"
                    android:scheme="appokesdk" />
            </intent-filter>
        </activity>

        <service
            android:name="com.appoke.sdk.CommunicationService"
            android:process=":AppokeSDK" >
        </service>   
        
        <receiver android:name=".TestReceiver">
			<intent-filter>
				<action android:name="com.appoke.sdk.APP_SHARED" />
				<data
                    android:host="@string/package_name"
                    android:scheme="appokesdk" />
			</intent-filter>
		</receiver>
    </application>

   			
    <!-- Used for install referrer tracking -->
	<receiver android:name="com.appoke.sdk.AppokeTrackingReceiver" android:exported="true">
	  <intent-filter>
	    <action android:name="com.android.vending.INSTALL_REFERRER" />
	  </intent-filter>
	  <meta-data android:name="forward.GoogleAnalytics"
                      android:value="com.google.android.apps.analytics.AnalyticsReceiver" />
		
	</receiver>
        
        
</manifest>